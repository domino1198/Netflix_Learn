import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    img:{
        width: '40%',
        aspectRatio: 16 / 9,
        resizeMode: 'cover',
    },
    title: {
        fontSize: 12,
    },
    duration: {
        fontSize:10,
        opacity:0.6,
    },
    plot:{
        opacity: 0.6,
    },

});

export default styles;